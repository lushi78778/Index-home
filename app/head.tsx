/**
 * @file 自定义 <head> 片段
 * @description
 * 该组件用于在 App Router 的文档 <head> 中插入额外的标签，
 * 如预连接（preconnect）、DNS 预解析（dns-prefetch）等优化标签。
 */
export default function Head() {
  return <>{/* 预留：可按需添加第三方的 preconnect/dns-prefetch */}</>
}
